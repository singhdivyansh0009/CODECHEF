#include <iostream>
using namespace std;

int main() {
	int t,a,b,p,q;
	cin >> t;
	while(t--)
	{
	  cin >> a >> b;
	   p=(a*100)/10;
	   q=(b*100)/20;
	   if(p>q)
	     cout << "FIRST" << endl;
	   else
	     if(q>p)
	       cout << "SECOND" << endl;
	     else
	       cout << "ANY" << endl;
	}
	return 0;
}
